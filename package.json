{
  "name": "webrtc-server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "pnpm build",
    "start": "concurrently --kill-others \"pnpm watch\" \"nodemon .\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2",
    "tslint": "^6.1.3",
    "typescript": "^5.3.3"
  }
}
